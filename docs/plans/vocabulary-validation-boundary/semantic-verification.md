# Semantic verification — LeafReader validation boundary candidate

## Disposition

Semantic result: **PASS**.

This is a complex-plan creation, not a preservation revision. The H1-only `base-scaffold.md` exists solely so the mechanical guard can verify the closed creation delta; it is explicitly not a prior canonical plan. The candidate must not be described as canonical until the user accepts it.

## Delta pass

- All 14 delta items have a mapped candidate section and ledger requirement IDs.
- All 66 plan requirement anchors occur exactly once and map one-to-one to the 66 target-ledger records.
- The target ledger records 60 active, 3 deferred, and 3 superseded requirements.
- The refined recommendation—not the earlier broad sketch—controls the candidate: one internal target now; CLI, generic support, and separate-package choices remain deferred or superseded as recorded.

## Preservation and source-integrity pass

- All 22 entries in `source-lock.json` were rehashed successfully. The baseline lock is historical: future repository-source verification uses the recorded Git tree, while the external user brief uses its locked attachment bytes.
- The candidate changes no product source, test source, runner, schema, protocol, manifest, reservation, report, or evidence artifact.
- Existing authoritative product/scientific contracts remain external, immutable, hash-identified sources rather than being paraphrased into a replacement contract.
- Repository changes are confined to the new `docs/plans/vocabulary-validation-boundary/` directory.

## Exact-contract pass

- The mechanical guard approved every numeric and versioned token introduced by the creation delta; no token was inherited from conversational memory.
- The candidate preserves the explicitly named 512 production sample count and 98% claim context without changing either.
- The seven review decision groups remain decisions for the independent reviewer; the candidate does not invent a margin, estimator, interval, confidence level, timing value, sample size, or privacy policy.
- The source revision, target names, existing type/file names, command names, ordering, failure actions, and sealed-set rules are explicit.
- The original sealed generator hashes remain unchanged in the manifest; an approved additive binding or distinct fresh reservation is required before pinned generator sources move. Neither route may weaken the historical seal or grant outcome access.

## Negative-control pass

- The candidate contains none of the three forbidden superseded positive directives.
- It explicitly rejects the unapproved model/study changes in `NEG-001`.
- A CLI target, a generic experiment framework, and a separate package/repository are not part of the initial extraction.
- Development-confirmation, release-holdout, consumed POS held-out cases, and human-collection paths are excluded from baseline/parity scoring. Existing read-only validation of the consumed POS fixture/report remains permitted solely as an integrity check.

## Cross-interface pass

1. Production facts flow from `LeafReaderCore` through one immutable package-scoped snapshot to `LeafReaderValidation`; experiment semantics, the mixed bank-error cases, and selected-set overrides do not flow back into Core or App.
2. Validation Swift code executes the production model; thin scripts perform command/file composition; Python consumes artifacts for statistics, comparison, and schema validation.
3. The SwiftPM target graph plus import/API checks must prove that the shipping `LeafReaderApp` product cannot reach Validation directly or transitively; the standalone evaluator/POS wrappers also need explicit same-package build/link integration.
4. Refactorable validation software produces versioned artifacts, while current protocols/reservations/evidence remain at stable paths and under stable hashes.
5. Scientific order remains review, two developmental studies, warm-path decision, candidate freeze, one reviewed active development-confirmation use, final release holdout, then real-learner validation; engineering evidence and extraction may run in the declared parallel window only. The early GUI timing matrix is diagnostic and cannot replace the post-freeze final-tree performance gates.
6. Failure paths preserve adverse evidence, return to fresh development inputs, and never convert a viewed reservation into untouched evidence.

## Standalone pass

An implementer can identify the selected target graph, ownership rules, exact move/stay classifications, five implementation slices, parity comparison, sealed generator-lock decision point, commands, protected inputs, and stop conditions from the candidate alone. Underlying product/scientific values intentionally remain governed by the locally available hash-locked sources in `source-lock.json`; the candidate does not replace or weaken them.

## Independent verification

The first fresh-verifier pass found a sealed generator-lock conflict, standalone Swift wrapper linkage gap, early-versus-final performance-gate ambiguity, missing clean-tree/reviewed-consumption prerequisites, mixed Core/Validation errors and selection override, and a Commit 2 dependency-order failure. Each finding was checked against source and repaired in the candidate, ledger, and delta. The final independent recheck returned **PASS** on candidate SHA-256 `0f85e0d18094eb2efccaa601e0c3f9dff59a6ab563fdc5c838bc30e1b21b1467` with no remaining material finding.
