# Semantic verification — reviewed candidate revision

## Authority and three-way comparison

Base status: verified but unaccepted candidate, not an accepted canonical plan. The immutable base is `base-plan.md` at SHA-256 `0f85e0d18094eb2efccaa601e0c3f9dff59a6ab563fdc5c838bc30e1b21b1467`; the raw review is `feedback.md` at SHA-256 `7867a698953b7264c4b6c0f6a453e714e751a3728e8fd3c3290c058a92142101`. The candidate is `ee61a39745657e95dc6999bcda9bd15c0aa0b2d315221b65e8502be6b14fff9a`. No user acceptance is inferred from the review's conditional endorsement.

## Delta pass

All 11 delta entries are implemented in their mapped sections: candidate status; selected fresh-confirmation route; 2A–2E staged migration; minimal Core snapshot; hard-versus-advisory architecture enforcement; human-study execution boundary; validation-subsystem wording; frozen machine-readable parity manifest and its baseline step; App cross-format test ownership; and finite migration controls. The candidate has 68 unique requirement anchors matching the 68 target-ledger records. The 17 changed or added ledger IDs are precisely the IDs authorized by the delta; all other base records remain structurally identical.

## Preservation and exact-contract pass

- The original one-way target boundary, no product/App dependency, single production CAT, Python statistical role, script interfaces, historical archive, strict Swift checks, performance gates, and eight scientific/release gates remain operationally stated.
- Existing generator/reservation hashes and seeds are not amended. The old reservation remains `reservedNotExecuted`; the later fresh disjoint reservation is a new sealed artifact, not a rebound or relabeled old set. No concrete new seed, threshold, estimator, protocol ID, or numerical policy was invented.
- The prior Commit 2 obligations are redistributed, not dropped: study-schema compatibility, Core-only observation errors, Validation bank errors, selected-set ownership, same-package wrapper build/link, output parity, and integrated evaluator self-test appear across 2A–2E.
- Source/path-independent JSON/Markdown parity and exit status remain exact; the new parity manifest freezes the inputs and allowed canonicalization before source edits. It cannot normalize metrics or absorb failed runs.
- The pre-release final-tree 150 ms algorithm, 16 ms main-thread, and document-open control + max(10%, 50 ms) requirements remain unchanged; the early matrix remains developmental only.

## Negative-control pass

- No production CAT copy, new public package, App-to-Validation dependency, immediate CLI target, generic ExperimentKit, model safeguard, warm-weight change, production sample-count change, held-out POS tuning, confirmation peek, or release-holdout reuse was introduced.
- The previous additive-rebinding option is explicitly inactive for this extraction; no `development-confirmation-v2` identifier or seed value is minted in the plan.
- Temporary validation-only evaluator duplication is scoped to 2B–2D and removed in 2E. The hard dependency/import/link checks remain in force throughout; broad naming checks cannot fail on comments or unrelated words.

## Cross-interface pass

1. Freeze the finite parity manifest before editing source; migrate the historical validator and prove Git-blob integrity before pinned generator files move.
2. In 2B–2C, old and new bank evaluators coexist and compare identical production states; 2D switches the wrapper only after exact parity; 2E removes the old Core experiment code. Each slice has a green test boundary.
3. Core emits only production observations, including minimal item fields; Validation interprets banks and counterfactual selected sets. App integration fixtures remain in App tests and cannot force Validation to depend on App.
4. At the extraction checkpoint, the historical seal and extracted-generator provenance pass. Only after extraction and parity is a fresh disjoint confirmation reservation sealed against the extracted generator; its new validator then becomes mandatory before candidate freeze. Confirmation still needs the clean-tree/model/resource/analysis bindings and a separate reviewed decision to consume that specific reservation.
5. Human developmental collection may start after independent approval and freezing the relevant study execution path, without waiting for unrelated POS/cross-format housekeeping. No data-generating or interpreting path may move while its study collects.
6. Migration records remain archived, while temporary allowances and duplicate evaluator code expire; only the one-way production/validation architecture endures.

## Standalone and mechanical pass

The candidate specifies the selected reservation route, exact order and stop conditions, module ownership, narrow snapshot, validation wrapper transition, parity manifest, verification commands, and human-study preconditions without referring to an unavailable conversational answer. Its `source-lock.json` is present beside the candidate and byte-identical to the earlier historical lock. `plan_guard.py` reports PASS: 20 changed sections, 6 added, 1 removed, 0 unexpected changes, 62 active / 3 deferred / 3 superseded requirements, no errors or warnings.

Independent forward-test: **PASS** on candidate SHA-256 `ee61a39745657e95dc6999bcda9bd15c0aa0b2d315221b65e8502be6b14fff9a`, with no material preservation finding. The final candidate is verified but not canonical or accepted.
