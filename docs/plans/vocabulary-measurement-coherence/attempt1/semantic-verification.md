# Semantic verification report

## Inputs

- Base plan SHA-256: `868106f57e1fa78a0b0f03c3e4e0dd29d6161500ef6b8ed8d13bc05bcc6a1671`
- Review SHA-256: `f5299abc0c32aa5fb1af34068aaad74e069d08bb274c455f737e06de2379bc5c`
- Candidate SHA-256: `ba734ee8ea40337735584a7c93a52b4e5233f6305eb2f87bf474e8cada752e1b`
- Delta items: 20
- Base ledger: 30 active, 4 deferred
- Target ledger: 42 active, 12 deferred, 0 superseded

## Pass 1 — delta

**PASS.** Every `F-01` through `F-20` item is mapped exactly once in `delta.json`, has a payload source, stable requirement IDs, allowed section paths, and verification literals. The candidate explicitly includes:

- separated `εknowledge` and evidence-specific `ρ` parameters;
- the production categorical likelihood in the fitter;
- consented 5–10% calibration assignments and assignment metadata;
- established DIF methods and an optional later L1 residual;
- the frozen failing holdout and five-factor diagnostic matrix;
- time-diffused warm-prior research behind existing non-inferiority gates;
- English/German UD POS validation;
- residual domain evaluation and hierarchical lexical difficulty;
- bounded proficiency values and a separate consented study schema;
- projected-coverage wording, empirically gated 95%/98%/99% goals, and deferred burden modes;
- MWUs before book-local sense splitting;
- post-pilot empirical risk calibration;
- staged 128/512/1024 performance experiments without changing the 512 production default;
- a hash-locked replacement source for PR #9.

## Pass 2 — preservation and exact contracts

**PASS.** Mechanical ledger comparison reports no unauthorized requirement changes. The amended base IDs are `EVID-002`, `VALID-001`, `CAL-001`, `EXPORT-001`, and `SENSE-001`; all are authorized by the delta. All other base requirement records remain structurally equal.

The candidate retains the exact evidence coefficients; classification thresholds; Beta(1,19) procedure and 0.05–0.25 clamp; difficulty formulas; 121-point grid; 512 predictive samples; fifth percentile; 16-item shortlist; POS thresholds; version-3 migration; 90%/10% warm mixture and 0.35 smoothing; 180-day/two-session/40-answer/two-validation eligibility; domain gates; export exclusions; 100-learner/0.35-SE/0.01-2PL gates; synthetic quality gates; 150 ms and 16 ms performance gates; commit order; and final commands.

## Pass 3 — negative controls

**PASS.** The candidate does not enable:

- automatic upload or response telemetry;
- free-form self-rating as a production θ prior;
- domain blending;
- empirical calibration packs without review gates;
- 2PL;
- L1-conditioned difficulty;
- MWUs;
- same-POS sense splitting;
- conformal/risk calibration;
- 95%/99% product targets;
- quick/balanced cold modes;
- 1024 production samples.

The failed release holdout and failed warm performance repetition remain blockers; their gates are not weakened and the version-2 golden is not replaced.

## Pass 4 — cross-interface traces

**PASS.** The following producer → artifact → consumer chains are operational and non-contradictory:

1. Tail validation → `εknowledge` posterior → latent knowledge curve and stop confidence.
2. Independent criterion labels → evidence-specific `ρ` estimates/version → production and offline categorical likelihood.
3. Research calibration assignment → selection metadata and inclusion probability → item-centered calibration analysis.
4. Consented criterion/rater/audit records → `ConsentedValidationStudyDataset` → participant-and-document holdout evaluator.
5. Proper DIF analysis → reviewed item/L1 eligibility → inert or held-out-validated production pack.
6. General frequency plus lexical/domain residual features → hierarchical partial pooling → existing domain and subgroup gates.
7. Elapsed time plus frozen drift parameter → 121-grid convolution → existing warm mixture → warm non-inferiority gate.
8. Cheap answer update → optional 128-sample screen → full 512-sample stop/final decision → parity tests and 150 ms gate.
9. MWU labeled fixtures and overlap policy → MWU production gate → only then book-local sense proposals.
10. Frozen real validation split → optional outer risk calibration → separately gated deck safety margin.

## Pass 5 — standalone completeness

**PASS.** The candidate contains the formulas, thresholds, ordering, failure actions, privacy boundaries, enablement gates, and deferred decisions required to execute the roadmap. Its only external normative payload is the complete replacement PR description, which is copied beside the plan and cited by exact SHA-256.

## Bounded repair log

- `SEM-001` (attempt 1): the heading “Product behavior after empirical validation” conflicted with the immediate wording clarification. Renamed the section to distinguish immediate wording from empirically gated options.
- `SEM-002` (attempt 1): current adverse holdout/performance values were not explicit enough for a standalone implementer. Added the locked failure values and retained-gate actions from the review.
- `SEM-003` (attempt 1): the consented validation schema lacked the full independent criterion execution sequence. Added the frozen rubric, two blinded raters, audit designs, delayed retest, metrics, baselines, and participant/document holdout procedure.
- `SEM-004` (attempt 1): the PR-body source reference lacked immutable identity. Added the review artifact SHA-256.

No finding required a second repair attempt.

## Disposition

Semantic verification: **PASS**, pending independent forward verification.
