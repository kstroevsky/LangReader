name: Architecture

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deterministic-checks:
    runs-on: macos-15
    env:
      DEVELOPER_DIR: /Applications/Xcode_16.4.app/Contents/Developer
      HOMEBREW_NO_AUTO_UPDATE: "1"
    steps:
      - uses: actions/checkout@v4
      - name: Verify pinned toolchain
        run: |
          sw_vers
          xcodebuild -version
          swift --version
      - name: Install deterministic check dependencies
        run: |
          brew install ripgrep
          brew install --cask sparkle
          sparkle_home="$(find "$(brew --prefix)/Caskroom/sparkle" -mindepth 1 -maxdepth 1 -type d ! -name .metadata | LC_ALL=C sort | tail -n 1)"
          test -d "$sparkle_home/Sparkle.framework"
          echo "SPARKLE_HOME=$sparkle_home" >> "$GITHUB_ENV"
      - name: Run deterministic architecture, build, and test checks
        run: ./scripts/check.sh --no-build
